@model ResidentsDuesApp.Expenditure

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Expenditure</legend>

        @Html.HiddenFor(model => model.ExpenditureID)

        <div class="editor-label">
            @Html.LabelFor(model => model.ExpenditureDate)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ExpenditureDate)
            @Html.ValidationMessageFor(model => model.ExpenditureDate)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Expenditure1)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Expenditure1)
            @Html.ValidationMessageFor(model => model.Expenditure1)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Fee)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Fee)
            @Html.ValidationMessageFor(model => model.Fee)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
