@model IEnumerable<ResidentsDuesApp.Expenditure>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ExpenditureID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExpenditureDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Expenditure1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fee)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ExpenditureID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExpenditureDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Expenditure1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fee)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ExpenditureID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ExpenditureID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ExpenditureID })
        </td>
    </tr>
}

</table>
